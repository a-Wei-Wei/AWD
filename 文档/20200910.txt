1、.shell.shell
2、全局搜索危险函数：eval(、assert(、call_user_func(、
3、找到危险函数，需要回溯参数是否是用户的可控范围之类
4、bp, 修改请类型，是在右键里面 change request method
5、查看配置文件，可查到数据库的连接账户、密码等信息
6、由于数据库密码被加密，可看install文件夹下的，安装信息。可看到管理员信息的，一般加密过于复杂，可考虑sqli，找一个相对的简单的页面，进行分析，一般是 注册、修改信息、登录等
7、desc 表名： 查看数据结构
8、绕过单引号，'123456' 将 该字符串引号中的内容进行16进制编码，这样就insert into admin values(0x123,0x123);
9、白盒 + 黑盒
10、